import { CodeIcon } from "@heroicons/react/solid";
import React from "react";
import { projects } from "../data";

export default function Projects() {
  return (
    <section id="projects" className="text-gray-400 bg-gray-900 body-font">
      <div className="container px-50 py-50 mx-auto text-center lg:px-100">
        <div className="flex flex-col w-full mb-20">
          <CodeIcon className="mx-auto inline-block w-10 mb-4" />
          <h1 className="sm:text-4xl text-3xl font-medium title-font mb-4 text-white">
            My Projects
          </h1>
          <p className="lg:w-2/3 mx-auto leading-relaxed text-base">
            In the past year, I've actively embraced the challenge of building
            dynamic applications across multiple platforms and frameworks. From
            mobile development with my project Catch-Up, an app that is built
            using the Flutter SDK and leverages Firebase for real-time backend
            functionalities. My intrigue with multimedia processing is reflected
            in the app with the integration of the FFmpeg API, a testament to my
            quest for creating seamless user experiences. Transitioning to web
            applications, I've employed React for its component-based
            architecture for my project MovieLib, interfacing it with the IMDb API to craft a responsive
            and informative movie exploration platform. Keep scrolling to witness the
            culmination of dedication, rigorous coding, and incessant learning
            through my projects.
          </p>
        </div>
        <div className="flex flex-wrap -m-4">
          {projects.map((project) => (
            <a
              href={project.link}
              key={project.image}
              className="sm:w-1/2 w-100 p-4"
            >
              <div className="flex relative">
                <img
                  alt="gallery"
                  className="absolute inset-0 w-full h-full object-contain object-center"
                  src={project.image}
                />
                <div className="px-8 py-10 relative z-10 w-full border-4 border-gray-800 bg-gray-900 opacity-0 hover:opacity-100">
                  <h2 className="tracking-widest text-sm title-font font-medium text-green-400 mb-1">
                    {project.subtitle}
                  </h2>
                  <h1 className="title-font text-lg font-medium text-white mb-3">
                    {project.title}
                  </h1>
                  <p className="leading-relaxed">{project.description}</p>
                </div>
              </div>
            </a>
          ))}
        </div>
      </div>
    </section>
  );
}
